require 'rake'
require 'rspec/core/rake_task'
require './app'

namespace :db do
  task :migrate do
    # Ejecutar migraciones de la base de datos
  end
end

task :spec do
  RSpec::Core::RakeTask.new(:spec)
end
